#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
    int a, b, i;
    if (scanf("%d\n%d", &a, &b) == 2) {
        char* words[] = {"", "one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};
        
        for (i = a; i <= b; i++) {
            if (i >= 1 && i <= 9) {
                printf("%s\n", words[i]); // Fixed 'n' to 'i'
            }
            else if (i > 9) {
                if (i % 2 == 0) {
                    printf("even\n");
                } else {
                    printf("odd\n");
                }
            }
        }
    }

    return 0;
}
