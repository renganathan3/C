#include <stdio.h>

int main() {
    int T, A, B;
    
    // Read the number of test cases
    if (scanf("%d", &T) != 1) return 0;
    
    // Loop through each test case
    while (T--) {
        scanf("%d %d", &A, &B);
        printf("%d\n", A + B);
    }
    
    return 0;
}
